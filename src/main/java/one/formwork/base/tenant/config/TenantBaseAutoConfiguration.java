package one.formwork.base.tenant.config;

import org.springframework.boot.autoconfigure.AutoConfiguration;

@AutoConfiguration
public class TenantBaseAutoConfiguration {
    // Stub: real implementation lives in the internal formwork-base-tenant
    // artifact, which wasn't available for this take-home. Only used here
    // as an ordering anchor (`after = TenantBaseAutoConfiguration.class`).
}
